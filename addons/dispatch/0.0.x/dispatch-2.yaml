# Do NOT edit this file directly.
# File generated from clusteraddon.yaml.tmpl by the update-gitops-repo tool with arguments: /usr/bin/update-gitops-repo -branch= -create-pull-request=false '-commit-message='"'"'update image digests to 0.0.0-test'"'"'' -substitute=chartVersion=0.0.0-test -substitute=chartRepo=https://mesosphere.github.io/dispatch -substitute=minioDefaultSize=50Gi -substitute=buildName=0-0-0-test -substitute=dispatchImage=mesosphere/dispatch:0.0.0-test -substitute=dispatchTestImage=mesosphere/dispatch-helm-test:0.0.0-test -substitute=helmImage=mesosphere/dispatch-helm:0.0.0-test -substitute=kindImage=mesosphere/dispatch-kind:0.0.0-test -substitute=dindImage=mesosphere/dispatch-dind:0.0.0-test -substitute=testutilsImage=mesosphere/dispatch-testutils:0.0.0-test -substitute=entrypointImage=mesosphere/dispatch-entrypoint:0.0.0-test -substitute=gsutilImage=mesosphere/dispatch-gsutil:0.0.0-test -substitute=docsImage=mesosphere/dispatch-docs:0.0.0-test -substitute=buildkitMemory=4Gi -substitute=buildkitCPU=4000m -substitute=buildkitEnabled=false -compile-only -git-ignore -verbose

apiVersion: kubeaddons.mesosphere.io/v1beta2
kind: ClusterAddon
metadata:
  name: dispatch
  labels:
    kubeaddons.mesosphere.io/name: dispatch
    kubeaddons.mesosphere.io/hack-requires-defaultstorageclass: "true"
  annotations:
    catalog.kubeaddons.mesosphere.io/addon-revision: "0.0.0-test"

    # Dispatch
    appversion.kubeaddons.mesosphere.io/dispatch: "0.0.0-test"
    endpoint.kubeaddons.mesosphere.io/dispatch: /dispatch/dashboard 
    docs.kubeaddons.mesosphere.io/dispatch: "https://docs.d2iq.com/dkp/dispatch/latest/"

    # Tekton
    appversion.kubeaddons.mesosphere.io/tekton: "0.14.2"
    endpoint.kubeaddons.mesosphere.io/tekton: /dispatch/tekton/
    docs.kubeaddons.mesosphere.io/tekton: "https://docs.d2iq.com/dkp/dispatch/latest/"

    values.chart.helm.kubeaddons.mesosphere.io/dispatch: "https://raw.githubusercontent.com/mesosphere/charts/master/stable/dispatch/values.yaml"
spec:
  namespace: dispatch
  kubernetes:
    minSupportedVersion: v1.15.6
  cloudProvider:
    - name: aws
      enabled: false
    - name: azure
      enabled: false
    - name: gcp
      enabled: false
    - name: vsphere
      enabled: false
    - name: docker
      enabled: false
    - name: none
      enabled: false
  requires:
    - matchLabels:
        kubeaddons.mesosphere.io/name: prometheus
    - matchLabels:
        kubeaddons.mesosphere.io/name: cert-manager
  chartReference:
    chart: dispatch
    repo: "https://mesosphere.github.io/dispatch"
    version: "0.0.0-test"
    values: |
      ---
      dispatch:
        image: "mesosphere/dispatch:0.0.0-test"
        testImage: "mesosphere/dispatch-helm-test:0.0.0-test"
      tekton:
        controller:
          images:
            entrypoint: "mesosphere/dispatch-entrypoint:0.0.0-test"
            gsutil: "mesosphere/dispatch-gsutil:0.0.0-test"
        webhook:
          replicas: 3 # tekton webhook
      global:
        prometheus:
          enabled: true
          release: prometheus-kubeaddons
        grafana:
          enabled: true
          namespace: kubeaddons
      minio:
        persistence:
          size: "50Gi" # minio
      buildkit:
        enabled: false # buildkit
        resources:
          limits:
            cpu: "4000m" # buildkit
            memory: "4Gi" # buildkit
          requests:
            cpu: "4000m" # buildkit
            memory: "4Gi" # buildkit
      flux:
        helmRepo: "https://mesosphere.github.io/dispatch"
